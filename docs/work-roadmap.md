# 开发记录

### 2026-09-21 - GitHub 一键更新

- 本次目标：像 Codex 一样在应用内检查并安装 GitHub 最新正式版。
- 实际改动：托盘与命令面板新增“检查更新”；正式安装版严格匹配 Release 安装包、校验 SHA-256、暂存验证后切换目录，失败自动回滚；源码安装版仅在 `main` 工作区干净时快进拉取、测试并同步。
- 数据保护：更新保留 `commands.json`、`commands.local.json`、迁移备份和 `usage.json`，不覆盖用户命令与使用统计。
- 验证重点：更新目录切换成功路径、损坏新版自动回滚、PowerShell 脚本语法与完整构建测试。

### 2026-09-20 - 生产化安装与发布自动化

- 本次目标：让用户「下载即装」即可使用，并打通首个 GitHub Release。
- 实际改动：新增首次运行引导安装（免管理员写入 HKCU 右键菜单、桌面/开机快捷方式，安装到 `%LOCALAPPDATA%\CLIList`）；托盘新增版本展示与一键卸载；`install.ps1`/`uninstall.ps1` 改为 HKCU 免管理员并清理旧 HKLM 残留；共享配置净化（本机路径命令迁入 `commands.local.json`）；新增 `{context:q}`、`{appdir}`/`{appdir:q}` 占位符；程序集版本 0.1.0；新增 tag 触发 Release workflow（zip + 变更说明 + GitHub Release）。
- 验证重点：构建与配置校验、安装版合并配置校验、HKCU 注册表写入、旧 HKLM 清理、驻留进程重启。
- 修复记录：工作区原有拖拽排序/自绘滚动条代码存在三处编译错误（`exitItem` 未声明、`Control.BorderStyle`、`ValueChanged` 参数），已修复。

### 2026-09-13 - 快捷键窗口前置修复

- 本次目标：解决快捷键触发后窗口被当前应用遮挡的问题。
- 实际改动：通过 Windows 原生窗口 API 恢复、置顶并聚焦命令面板。
- 验证重点：保持 ChatGPT/Codex 前台时触发快捷键，确认前台窗口切换到 CLI List。

### 2026-09-13 - Git 拉取后自动更新

- 本次目标：多台 Windows 设备首次安装后，仅通过 `git pull` 完成程序更新。
- 实际改动：增加版本化 Git Hook、自动构建验证、安装文件同步、驻留进程重启及共享/本机配置分层。
- 数据保护：首次迁移保留旧配置备份，本机差异写入 `commands.local.json`，使用统计保持本地。
- 验证重点：PowerShell 语法、程序构建、共享配置、本机覆盖配置和安装版校验。

### 2026-09-12 - 全局快捷键与托盘驻留

- 本次目标：从任意位置快速呼出 CLI List，并避免重复进程。
- 实际改动：增加 `Ctrl + Alt + Space`、托盘菜单、开机启动、单实例和 Named Pipe 目录转交。
- 验证重点：构建与配置校验、快捷键显示/隐藏、重复启动和托盘退出。

### 2026-09-08 - 首个开源版本

- 本次目标：将本机 CLI List 整理为可公开发布的独立项目。
- 实际改动：迁移 Windows Forms 源码、构建与安装脚本；加入通用示例配置、极简像素界面、目录预览、测试脚本和 Windows CI。
- 验证结果：本机构建成功，配置校验退出码为 `0`，主窗口和目录窗口截图验收通过。
- 遗留问题：尚未提供图形化命令配置编辑器。
- 下一步：发布首个 GitHub Release，并收集安装反馈。
