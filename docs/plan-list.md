# 后续计划

## 整体计划

保持单文件 Windows 应用定位，优先提升配置体验、安装稳定性和发布质量。

## 当前进行中

- 收集首个可下载版本的安装反馈。

## 下一步计划

1. 增加图形化命令配置编辑器。
2. 增加配置导入、导出和手动排序能力。
3. 完善不同 Windows 版本的安装验证。
4. 提供 Release 自动更新检查（托盘“检查新版本”）。

## 阻塞项 / 依赖项

- 首个 Release 已具备：稳定版本号（0.1.0）与变更说明由 Release workflow 自动生成。

## 已完成里程碑

- 完成下载即装的首次运行引导安装（免管理员 HKCU 注册）。
- 完成托盘一键卸载与版本号展示。
- 完成 tag 触发 Release 自动化（zip + 变更说明 + GitHub Release）。
- 完成共享配置净化（本机路径命令迁入 `commands.local.json`）。
- 完成一次安装、后续 `git pull` 自动更新的多端同步流程。
- 完成共享命令与本机命令覆盖分层。
- 完成托盘驻留、全局快捷键、单实例与当前目录转交。
- 完成可配置命令面板。
- 完成资源管理器右键菜单集成。
- 完成目录浏览和文件预览。
- 完成极简像素风界面。
- 完成 CLI 搜索、标签筛选与本地使用频率统计。
